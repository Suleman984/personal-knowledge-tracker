# 📘 Personal Knowledge Tracker

Personal Knowledge Tracker (PKT) is a full-stack project built with:
- **Backend:** Golang (Gin/Fiber/Echo) + Postgres + Redis
- **Frontend:** Next.js (TypeScript + Tailwind)
- **Extension:** Chrome Extension (Manifest V3)
- **Infra:** Docker Compose (Postgres, Redis, MinIO)

---

go get github.com/gin-gonic/gin
go get github.com/jmoiron/sqlx          # DB helper
go get github.com/lib/pq               # Postgres driver
go get github.com/redis/go-redis/v9    # Redis client
go get github.com/golang-jwt/jwt/v5    # JWT auth

